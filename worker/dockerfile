# This Dockerfile sets up a Python environment for the FluxFrame worker with Blender.

FROM python:3.11-slim

# Install Blender and system dependencies
RUN apt-get update && apt-get install -y \
    blender \
    wget \
    xvfb \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# Set environment variable for Blender path
ENV BLENDER_PATH=blender

CMD ["python", "src/main_api.py"]