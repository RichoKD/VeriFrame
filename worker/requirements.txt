starknet_py>=0.21.0,<0.28.0
ipfshttpclient>=0.8.0a2
requests>=2.25.0
# Ensure we have all required dependencies for starknet_py
aiohttp>=3.8.0
# For Blender rendering in headless mode, Blender should be installed on the system
# Additional packages for image processing and validation
Pillow>=10.0.0